# Playwright API Mocking

## Install
npm install
npx playwright install

## Run
npx playwright test

## Concept
Use page.route() to intercept API calls  
Use route.fulfill() to return mock data  

Result: UI works without real backend 🚀